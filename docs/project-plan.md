# DoTo 프로젝트 개발 계획서

> AI 코칭 연동형 게이미피케이션 목표달성/인생설계 앱

---

## 📊 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | DoTo |
| **유형** | 게이미피케이션 목표 관리 앱 (Goal → Project → Task/Habit) |
| **캐릭터** | 새싹 (Sprout) - 4단계 진화 |
| **다국어** | 기본 한국어 + 영어 (i18n) |
| **기술 스택** | Next.js 15 + TypeScript + Zustand + Tailwind CSS |
| **데이터베이스** | SQLite (개발) → PostgreSQL (프로덕션) |
| **배포 플랫폼** | Vercel |
| **개발 기간** | 4주 (MVP) + 확장 |
| **현재 상태** | 프론트엔드 프로토타입 완성 + 캐릭터 에셋 준비 완료 |
| **다음 단계** | MVP 핵심 루프 완성 (체크 → 보상 → 진화 + 다국어) |

---

## 🎯 한 줄 정의 (MVP 기준)

사용자가 **목표(3개월) → 프로젝트(4주) → 습관/과업(일)**을 운영하고,
**Today 체크 → 보상(Exp/코인) → 캐릭터 성장(레벨/진화)**로 실행을 지속하게 하는 앱.

---

## 🏗️ 서비스 운영 엔진 (핵심 규칙)

### 계층 구조 (고정)

| 레벨 | 항목 | 기간 | 역할 | 상태 |
|------|------|------|------|------|
| 1 | **Goal (목표)** | 기본 3개월 (시즌/분기) | 프로젝트 생성 상위 단위, 진행률 기준 | ✅ 완료 |
| 2 | **Project (프로젝트)** | 기본 4주 (스프린트) | 유저가 "진행 중"으로 체감하는 실행 단위 | ✅ 완료 |
| 3 | **Task/Habit (습관/과업)** | 일 단위 | 실행 항목 (체크 가능) | ✅ 완료 |
| 4 | **Daily Log (일일 기록)** | 날짜별 | 체크 결과 저장 | 🚧 구현 필요 |

### Habitica와의 차이점

| 구분 | Habitica | DoTo |
|------|----------|--------|
| **핵심** | todo/habit/reward 중심 | **Goal(3개월)–Project(4주)–Daily** 운영 |
| **성장** | 경험치 누적 | streak/완료 배지로 강화 (MVP+) |
| **AI 코칭** | 없음 | V2에서 연동 예정 |

---

## 📦 범위 정의: MVP / MVP+ / V2

> **원칙**: MVP는 "핵심 루프가 무조건 도는 것"만. CTO 합의 없이 MVP에 기능 추가 금지.

### MVP 범위 (AI 코칭 포함 모든 확장 제외)

#### A. 핵심 루프 (필수)
- [ ] Goal(3개월) 생성/조회 (수정/삭제는 선택)
- [ ] Project(4주) 생성/조회 (수정/삭제는 선택)
- [ ] Task/Habit CRUD (생성/조회/수정/삭제)
- [ ] Today 화면에서 Task/Habit 체크(완료)
- [ ] 체크 시 **보상 지급(Exp/코인)** + 캐릭터 상태 즉시 반영
- [ ] **streak(연속일) 계산**
- [ ] 기본 통계(최소): 오늘 완료 개수, 최근 7일 달성률
- [ ] **다국어 지원 (한국어/영어)**: 모든 UI 텍스트 언어팩 기반

#### B. 캐릭터 (최소 사양)
- [ ] 캐릭터 1종 고정: **새싹 캐릭터 (Sprout)**
- [ ] 상태값: `stage(진화단계) / level / exp / coins`
- [ ] 진화 단계별 이미지 (4단계)

#### C. 보상 체계 (고정값)
**난이도 3단계**: `Easy / Normal / Hard`

| 난이도 | 가중치 | Exp | Coin |
|--------|--------|-----|------|
| Easy | 1.0 | +10 | +3 |
| Normal | 1.5 | +15 | +5 |
| Hard | 2.0 | +20 | +6 |

**보상 계산 공식**:
- `Exp = 10 × 가중치`
- `Coin = 3 × 가중치` (반올림)

#### D. 진화 (최소 조건)
**Stage**: `Egg → Sproutling → Blooming → Fully Grown`

| 진화 | 레벨 조건 | 이미지 파일 |
|------|----------|------------|
| Egg → Sproutling | Lv 3 | level_1_egg.png → level_2_sproutling.png |
| Sproutling → Blooming | Lv 8 | level_2_sproutling.png → level_3_blooming.png |
| Blooming → Fully Grown | Lv 15 | level_3_blooming.png → level_4_fullygrown.png |

---

### MVP+ (있으면 좋지만 MVP에서 제외)
- [ ] **배지 시스템** (streak/프로젝트 완료 등)
- [ ] 진화 조건 강화 (Lv + 배지 혼합)
- [ ] 코인 소비처: 상점/아이템/스킨 (최소 3종)
- [ ] Insights 탭 분리 (주간 리포트/Top 습관)
- [ ] 푸시 알림/리마인더
- [ ] 데이터 내보내기/백업, 계정 연동
- [ ] 추가 언어 지원 (일본어, 중국어)

---

### V2 (확장)
- [ ] **AI 코칭 (Claude/OpenAI 연동)**
- [ ] 커뮤니티/파티/길드
- [ ] 퀘스트 체인/장비 슬롯 확장
- [ ] 캐릭터 다종 수집 (포켓몬식)
- [ ] 유료화 (구독/Gem)
- [ ] AI 기반 자동 번역 (추가 언어 확장)

---

## 📱 화면 구성 (IA)

> MVP는 5화면 고정. "화면 수 최소"가 목표.

### MVP 화면 (5개)

#### 1. Onboarding (초간단)
**구현 상태**: 🚧 미구현

**화면 구성**:
```
- 닉네임 입력
- (선택) 기본 Goal 1개 + Project 1개 + Habit 1~3개 생성 유도
- 스킵 가능
```

**구현 필요 사항**:
- [ ] 닉네임 입력 폼
- [ ] 초기 데이터 생성 플로우
- [ ] 스킵 기능

---

#### 2. Today (핵심 화면)
**구현 상태**: ✅ 기본 레이아웃 완료

**화면 구성**:
```
상단 헤더:
- 캐릭터 카드 (stage/level/exp/coins)
- 오늘 날짜

Today 체크리스트:
- 각 항목: 이름 / 연결 프로젝트 / 난이도 / 체크 버튼
- 체크 시: 보상/레벨업/진화 결과 즉시 반영

통계 (최소):
- 오늘 완료 개수 / 전체 개수
- 최근 7일 달성률
```

**구현 필요 사항**:
- [ ] 체크 버튼 인터랙션
- [ ] 보상 지급 로직
- [ ] 레벨업/진화 애니메이션
- [ ] 실시간 통계 업데이트

---

#### 3. Task/Habit 생성·편집
**구현 상태**: 🚧 부분 완료

**화면 구성**:
```
폼 필드:
- 이름 (필수)
- 설명 (선택)
- 타입 (task/habit)
- 반복 (기본 매일; 고급 반복은 MVP+)
- 난이도 (Easy/Normal/Hard)
- 연결 Project (드롭다운)

버튼:
- 저장
- 취소
```

**구현 필요 사항**:
- [ ] 폼 유효성 검증
- [ ] Project 선택 드롭다운
- [ ] 수정/삭제 기능

---

#### 4. Goals/Projects 목록
**구현 상태**: 🚧 미구현

**화면 구성**:
```
Goal 카드:
- 제목
- 시즌 기간 (3개월)
- 진행률 (%)

Project 리스트:
- 제목
- 4주 기간
- 상태 (active/done/paused)
- 연결된 Goal
```

**구현 필요 사항**:
- [ ] Goal 카드 컴포넌트
- [ ] Project 리스트 컴포넌트
- [ ] 진행률 계산 로직
- [ ] 상세 화면 (선택)

---

#### 5. Character
**구현 상태**: ✅ 기본 완료

**화면 구성**:
```
캐릭터 표시:
- 현재 Stage 이미지 (Egg/Sproutling/Blooming/Fully Grown)
- Level 표시
- Exp 바 (현재/다음 레벨)
- Coins 표시

(아이템/상점은 MVP+)
```

**구현 필요 사항**:
- [ ] 진화 단계별 이미지 (4단계)
- [ ] Exp 바 진행 애니메이션
- [ ] 진화 전환 애니메이션

---

## 🗂️ 데이터 모델 (최소 엔티티)

> 형태: RDB/NoSQL 모두 가능. 아래는 엔티티 기준.

### 엔티티 목록

#### User
```yaml
id: string (PK)
nickname: string
language: string (default: 'ko')
  # 'ko': 한국어, 'en': 영어
created_at: datetime
```

#### Goal
```yaml
id: string (PK)
user_id: string (FK)
title: string
season_start: date
season_end: date
status: enum(active|archived)
```

#### Project
```yaml
id: string (PK)
user_id: string (FK)
goal_id: string (FK)
title: string
start_date: date
end_date: date
status: enum(active|done|paused)
```

#### TaskHabit
```yaml
id: string (PK)
user_id: string (FK)
project_id: string (FK)
type: enum(task|habit)
title: string
description: string (optional)
difficulty: enum(E|N|H)
recurrence: string (MVP: daily 고정)
is_active: boolean
```

#### DailyLog
```yaml
id: string (PK)
user_id: string (FK)
date: date (local date)
```

#### DailyCheck
```yaml
id: string (PK)
daily_log_id: string (FK)
taskhabit_id: string (FK)
checked_at: datetime
reward_granted: boolean
exp_granted: number
coin_granted: number
```

#### Character
```yaml
user_id: string (PK, FK)
stage: number (1~4)
  # 1: Egg, 2: Sproutling, 3: Blooming, 4: Fully Grown
level: number
exp: number
coins: number
```

#### Badge (MVP+)
```yaml
user_id: string (FK)
badge_type: string
earned_at: datetime
```

---

### 핵심 트랜잭션 (원자성 필수)

**체크 버튼 클릭 프로세스**:

```
1. DailyLog(date) 없으면 생성
2. DailyCheck upsert (중복 클릭 방지)
3. 보상 계산 (Exp/Coin)
   - TaskHabit.difficulty 기반 가중치 적용
4. Character 업데이트 (exp/coins)
5. 레벨업 판정
   - requiredExp(level) = 100 + (level-1)*25
6. 진화 판정
   - stage 1→2 (Egg→Sproutling): Lv 3
   - stage 2→3 (Sproutling→Blooming): Lv 8
   - stage 3→4 (Blooming→Fully Grown): Lv 15
7. 결과 반환 (프론트 애니메이션용)
```

> **중요**: 체크 중복/오프라인/재요청 시 "보상 재지급" 막는 구조가 핵심.

---

## ⚡ 보상/성장 로직 (구현 규칙)

### 난이도 가중치
| 난이도 | 가중치 |
|--------|--------|
| Easy | 1.0 |
| Normal | 1.5 |
| Hard | 2.0 |

### 보상 계산
```typescript
// 기본 공식
exp = 10 × weight
coin = Math.round(3 × weight)

// 예시
Easy:   +10 Exp, +3 Coin
Normal: +15 Exp, +5 Coin
Hard:   +20 Exp, +6 Coin
```

### 레벨업 필요 Exp (권장 초안)
```typescript
requiredExp(level) = 100 + (level - 1) * 25

// 예시
Lv 1 → 2: 100 Exp
Lv 2 → 3: 125 Exp
Lv 3 → 4: 150 Exp
Lv 10 → 11: 325 Exp
```

**레벨업 방식** (구현 단순화):
- **누적형**: exp는 계속 누적, 레벨업 계산만 수행
- **차감형**: 레벨업 시 필요 exp 차감 (선택)

### 진화 규칙 (MVP)
| Stage | 레벨 조건 | 이미지 파일 |
|-------|----------|------------|
| 1 (Egg) → 2 (Sproutling) | Lv 3 | level_1_egg.png → level_2_sproutling.png |
| 2 (Sproutling) → 3 (Blooming) | Lv 8 | level_2_sproutling.png → level_3_blooming.png |
| 3 (Blooming) → 4 (Fully Grown) | Lv 15 | level_3_blooming.png → level_4_fullygrown.png |

---

## 📊 Streak/통계 (최소)

### Streak 정의 (권장)
- **증가 조건**: 하루에 최소 1개 체크하면 streak +1
- **끊김 조건**: 해당 날짜에 체크 0개

### MVP 통계 (최소 산출)
**Today 화면 표시**:
1. **오늘 완료 개수 / 전체 개수**
   - 예: `3 / 5`

2. **최근 7일 달성률** (간단):
   - 방법 1: `7일 동안 체크한 일수 / 7`
   - 방법 2: `7일 체크 횟수 / (7일 목표 체크 가능 횟수)`
   - **MVP는 구현 쉬운 방식(일수 기준)을 우선**

---

## ✅ Definition of Done (MVP 완료 기준)

사용자가:
1. ✅ Goal을 만들고
2. ✅ Project를 만들고
3. ✅ Habit/Task를 만들고
4. ✅ Today에서 체크하면
5. ✅ **Exp/코인 증가 + 레벨업/진화 반영 + streak/기본 통계가 정확히 계산**되어야 한다.

---

## 📅 개발 마일스톤 (4주 스프린트)

### Sprint 1 — 핵심 루프 완성 (필수)
**기간**: Week 1

**구현 내용**:
- [ ] **i18n 설정** (next-intl)
  - [ ] 언어 파일 구조 (ko.json, en.json)
  - [ ] 언어 전환 컴포넌트
  - [ ] 기본 언어: 한국어
- [ ] Today + 체크 버튼
- [ ] 보상 지급 로직
- [ ] Character exp/coin 반영
- [ ] Goal/Project/Task 최소 CRUD
- [ ] **모든 UI 텍스트 언어팩 적용**

**산출물**: "체크하면 성장하는" 데모 (v0.1) + 한/영 전환

---

### Sprint 2 — streak/진화/통계 (최소)
**기간**: Week 2

**구현 내용**:
- [ ] streak 계산
- [ ] 진화 조건 (레벨 기반, 4단계)
- [ ] Today 내 간단 통계 (오늘/7일)
- [ ] 레벨업 애니메이션
- [ ] 진화 애니메이션

**산출물**: "7일 운영 가능한" MVP (v0.2)

---

### Sprint 3 — 안정화/QA/배포 (내부)
**기간**: Week 3

**구현 내용**:
- [ ] 중복 지급 방지 강화
- [ ] 에러/엣지 케이스 처리
- [ ] 내부 배포 (TestFlight/내부 APK)
- [ ] QA 버그 수정

**산출물**: 내부 테스트 가능한 MVP (v0.3)

---

### Sprint 4 — 폴리싱/최종 배포 (선택)
**기간**: Week 4 (선택)

**구현 내용**:
- [ ] UI/UX 개선
- [ ] 성능 최적화
- [ ] 배포 준비 (프로덕션)

**산출물**: 배포 가능한 MVP (v1.0)

> **중요**: AI 코칭은 Sprint 4로 잡지 말고, MVP+ 또는 V2 트랙으로 분리.

---

## 🛠️ 기술 스택

### 프론트엔드
```yaml
코어:
  - Next.js 15 (App Router)
  - React 19
  - TypeScript 5

다국어:
  - next-intl 3 (Next.js App Router 최적화)
  - 지원 언어: 한국어 (ko), 영어 (en)
  - 기본 언어: 한국어

스타일링:
  - Tailwind CSS 3.4
  - Framer Motion 11 (애니메이션)

상태관리:
  - Zustand 5 (경량 상태 관리)
  - Zustand Persist (로컬 저장소)

아이콘:
  - React Icons 5

린팅:
  - ESLint 9
  - eslint-config-next
```

### 백엔드 (향후 구현)
```yaml
데이터베이스:
  - SQLite (개발 환경)
  - PostgreSQL (프로덕션)
  - Prisma ORM

인증:
  - NextAuth.js
  - OAuth (Google, GitHub)

API:
  - Next.js API Routes
  - RESTful API

배포:
  - Vercel (프론트엔드 + 서버리스 함수)
  - Vercel Postgres (데이터베이스)
```

---

## 📁 프로젝트 구조

```
DoTo/
├── src/
│   ├── app/                          # Next.js App Router
│   │   ├── [locale]/                 # 다국어 라우팅
│   │   │   ├── layout.tsx            # 로케일별 레이아웃
│   │   │   ├── page.tsx              # 메인 페이지 (Today)
│   │   │   ├── onboarding/           # 온보딩 화면
│   │   │   ├── goals/                # Goal/Project 목록
│   │   │   ├── tasks/                # Task/Habit 생성·편집
│   │   │   └── character/            # 캐릭터 화면
│   │   ├── layout.tsx                # 루트 레이아웃
│   │   ├── globals.css               # 전역 스타일
│   │   └── api/                      # API Routes (향후)
│   │       ├── auth/
│   │       ├── goals/
│   │       ├── projects/
│   │       ├── tasks/
│   │       └── daily/
│   │
│   ├── i18n/                         # 다국어 설정
│   │   ├── request.ts                # next-intl 설정
│   │   └── routing.ts                # 라우팅 설정
│   │
│   ├── components/                   # React 컴포넌트
│   │   ├── CharacterCard.tsx         # ✅ 캐릭터 카드
│   │   ├── StatsBars.tsx             # ✅ 스탯 바 (Exp/Coins)
│   │   ├── TodayChecklist.tsx        # 🚧 Today 체크리스트
│   │   ├── TaskItem.tsx              # 🚧 Task/Habit 항목
│   │   ├── GoalCard.tsx              # 🚧 Goal 카드
│   │   ├── ProjectList.tsx           # 🚧 Project 리스트
│   │   │
│   │   ├── modals/                   # 🚧 모달 컴포넌트
│   │   │   ├── AddTaskModal.tsx
│   │   │   ├── EditTaskModal.tsx
│   │   │   └── RewardModal.tsx       # 보상 표시
│   │   │
│   │   └── animations/               # 🚧 애니메이션
│   │       ├── LevelUpAnimation.tsx
│   │       └── EvolutionAnimation.tsx
│   │
│   ├── store/                        # Zustand 스토어
│   │   ├── usePlayerStore.ts         # ✅ 플레이어 상태 (Character)
│   │   ├── useGoalStore.ts           # 🚧 Goal 상태
│   │   ├── useProjectStore.ts        # 🚧 Project 상태
│   │   ├── useTaskStore.ts           # ✅ Task/Habit 상태
│   │   └── useDailyStore.ts          # 🚧 Daily Log 상태
│   │
│   ├── types/                        # TypeScript 타입
│   │   └── index.ts                  # ✅ 기본 타입 정의
│   │
│   ├── lib/                          # 유틸리티
│   │   ├── db.ts                     # 🚧 Prisma 클라이언트
│   │   ├── auth.ts                   # 🚧 NextAuth 설정
│   │   ├── rewards.ts                # 🚧 보상 계산 로직
│   │   ├── evolution.ts              # 🚧 진화 판정 로직
│   │   └── utils.ts                  # 헬퍼 함수
│   │
│   └── hooks/                        # Custom Hooks
│       ├── useLocalStorage.ts        # ✅ 로컬 스토리지
│       └── useStreak.ts              # 🚧 Streak 계산
│
├── messages/                         # 🚧 다국어 언어 파일
│   ├── ko.json                       # 한국어
│   └── en.json                       # 영어
│
├── prisma/                           # 🚧 Prisma 스키마
│   └── schema.prisma
│
├── img/                              # ✅ 캐릭터 이미지 (이미 존재)
│   ├── level_1_egg.png               # Stage 1: Egg
│   ├── level_2_sproutling.png        # Stage 2: Sproutling
│   ├── level_3_blooming.png          # Stage 3: Blooming
│   └── level_4_fullygrown.png        # Stage 4: Fully Grown
│
├── public/                           # 정적 파일
│   └── icons/
│
├── ref/                              # ✅ 참고 이미지
│   └── KakaoTalk_Photo_*.png
│
└── docs/                             # 문서
    └── project-plan.md               # ✅ 프로젝트 계획서
```

**범례**:
- ✅ 구현 완료
- 🔄 부분 구현
- 🚧 미구현

---

## 🌍 다국어 지원 (i18n)

### 지원 언어
```yaml
기본 언어: 한국어 (ko)
추가 언어: 영어 (en)
향후 확장: 일본어 (ja), 중국어 (zh) - MVP+
```

### 언어 파일 구조
```json
// messages/ko.json
{
  "common": {
    "save": "저장",
    "cancel": "취소",
    "delete": "삭제",
    "edit": "수정"
  },
  "character": {
    "level": "레벨",
    "exp": "경험치",
    "coins": "코인",
    "stage": {
      "egg": "알",
      "sproutling": "새싹",
      "blooming": "꽃피는",
      "fullyGrown": "완전 성장"
    }
  },
  "today": {
    "title": "오늘",
    "completed": "완료",
    "total": "전체",
    "checkTask": "체크하기"
  },
  "task": {
    "create": "작업 추가",
    "title": "제목",
    "description": "설명",
    "difficulty": {
      "easy": "쉬움",
      "normal": "보통",
      "hard": "어려움"
    }
  }
}

// messages/en.json
{
  "common": {
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "edit": "Edit"
  },
  "character": {
    "level": "Level",
    "exp": "Experience",
    "coins": "Coins",
    "stage": {
      "egg": "Egg",
      "sproutling": "Sproutling",
      "blooming": "Blooming",
      "fullyGrown": "Fully Grown"
    }
  },
  "today": {
    "title": "Today",
    "completed": "Completed",
    "total": "Total",
    "checkTask": "Check"
  },
  "task": {
    "create": "Add Task",
    "title": "Title",
    "description": "Description",
    "difficulty": {
      "easy": "Easy",
      "normal": "Normal",
      "hard": "Hard"
    }
  }
}
```

### 사용 예시
```typescript
// components/TaskItem.tsx
import { useTranslations } from 'next-intl';

export default function TaskItem() {
  const t = useTranslations('task');

  return (
    <button>{t('create')}</button>
  );
}

// Character 진화 단계
const t = useTranslations('character.stage');
const stageName = t(stage); // 'egg', 'sproutling', etc.
```

### 언어 전환
```typescript
// components/LanguageSwitcher.tsx
import { useLocale } from 'next-intl';
import { useRouter, usePathname } from 'next/navigation';

export default function LanguageSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();

  const switchLanguage = (newLocale: string) => {
    router.push(pathname.replace(`/${locale}`, `/${newLocale}`));
  };

  return (
    <select value={locale} onChange={(e) => switchLanguage(e.target.value)}>
      <option value="ko">한국어</option>
      <option value="en">English</option>
    </select>
  );
}
```

### 라우팅 설정
```typescript
// src/i18n/routing.ts
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['ko', 'en'],
  defaultLocale: 'ko'
});

// middleware.ts
import createMiddleware from 'next-intl/middleware';
import { routing } from './src/i18n/routing';

export default createMiddleware(routing);

export const config = {
  matcher: ['/', '/(ko|en)/:path*']
};
```

### 개발 가이드라인
```yaml
필수 규칙:
  - 모든 UI 텍스트는 언어 파일에서 관리
  - 하드코딩된 한글/영어 텍스트 금지
  - 숫자, 날짜 포맷도 로케일에 맞게 처리

권장 사항:
  - 번역 키는 명확하고 계층적으로 구성
  - 공통 텍스트는 'common' 네임스페이스 사용
  - 긴 텍스트는 변수 보간 활용

검증:
  - PR 전 양쪽 언어 모두 테스트
  - 누락된 번역 키 자동 검출
  - 언어별 스크린샷 확인
```

---

## 🎨 디자인 시스템

### 색상 팔레트
```css
/* Primary Colors */
--purple-500: #6366f1;     /* 메인 보라색 */
--purple-600: #4f46e5;     /* 어두운 보라색 */
--purple-100: #e0e7ff;     /* 연한 보라색 */

/* Status Colors */
--exp-yellow: #eab308;     /* Experience 바 */
--coin-gold: #f59e0b;      /* 코인 */

/* Difficulty Colors */
--easy-green: #22c55e;
--normal-yellow: #eab308;
--hard-red: #ef4444;

/* Background */
--bg-main: #f9fafb;
--bg-card: #ffffff;
--bg-purple: #8b5cf6;      /* 헤더 배경 */
```

### 타이포그래피
```css
/* Font Families */
font-family: 'Pretendard', -apple-system, system-ui, sans-serif;

/* Font Sizes */
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */
```

---

## 🎨 캐릭터 에셋

### 진화 단계 이미지 (4단계)
```yaml
Stage 1 - Egg (알):
  파일: level_1_egg.png
  레벨: 1-2
  설명: 시작 단계의 알

Stage 2 - Sproutling (새싹):
  파일: level_2_sproutling.png
  레벨: 3-7
  설명: 알에서 깨어난 어린 새싹
  진화 조건: Lv 3 도달

Stage 3 - Blooming (꽃피는):
  파일: level_3_blooming.png
  레벨: 8-14
  설명: 성장하여 꽃을 피우는 단계
  진화 조건: Lv 8 도달

Stage 4 - Fully Grown (완전 성장):
  파일: level_4_fullygrown.png
  레벨: 15+
  설명: 완전히 성장한 최종 단계
  진화 조건: Lv 15 도달
```

---

## 🔐 보안 고려사항

### 현재 (로컬 스토리지)
```yaml
위험요소:
  - 브라우저 로컬 스토리지 접근 가능
  - XSS 공격 취약
  - 데이터 조작 가능

완화방안:
  - 중요 데이터 없음 (게임 진행 상황만)
  - CSP (Content Security Policy) 적용
  - Input Sanitization
```

### 향후 (백엔드 연동)
```yaml
필수사항:
  - HTTPS 강제
  - JWT 토큰 인증
  - Rate Limiting
  - SQL Injection 방지 (Prisma ORM)
  - XSS 방지 (React 자동 이스케이프)
  - CSRF 토큰

데이터보호:
  - 비밀번호 해싱 (bcrypt)
  - 환경변수 관리 (.env)
  - API 키 보호
```

---

## 📊 성능 목표

### 웹 바이탈
```yaml
LCP (Largest Contentful Paint): < 2.5s
FID (First Input Delay): < 100ms
CLS (Cumulative Layout Shift): < 0.1
TTI (Time to Interactive): < 3.5s
```

### 번들 크기
```yaml
Initial Load: < 200KB (gzipped)
Total Bundle: < 500KB
```

### 최적화 전략
```yaml
코드 스플리팅:
  - 페이지별 동적 임포트
  - 컴포넌트 Lazy Loading
  - 언어별 메시지 분리 로딩

이미지 최적화:
  - Next.js Image 컴포넌트
  - WebP 포맷
  - Lazy Loading

캐싱:
  - API 응답 캐싱
  - Static Generation (SSG)
  - CDN 활용 (Vercel)
  - 언어 파일 브라우저 캐싱

다국어 최적화:
  - 사용자 언어만 로드 (lazy loading)
  - 번역 키 최소화
  - 공통 번역 재사용
```

---

## 🧪 테스트 전략

### 단위 테스트 (Jest)
```yaml
대상:
  - Zustand Store 로직
  - 보상 계산 로직 (rewards.ts)
  - 진화 판정 로직 (evolution.ts)
  - 유틸리티 함수
  - i18n 번역 키 유효성

주요 테스트 케이스:
  - 난이도별 보상 계산 (Easy/Normal/Hard)
  - 레벨업 조건 판정 (100 + (level-1)*25)
  - 진화 조건 판정 (Lv 3, 8, 15)
  - 언어 파일 누락 키 검증
  - 번역 키 일관성 검증

도구:
  - Jest
  - React Testing Library
```

### E2E 테스트 (Playwright)
```yaml
시나리오:
  - Onboarding → Goal → Project → Task 생성
  - Today 체크 → 보상 획득 → 레벨업
  - 7일 연속 체크 → streak 확인
  - 진화 조건 달성 → 4단계 진화 확인
    - Lv 3: Egg → Sproutling
    - Lv 8: Sproutling → Blooming
    - Lv 15: Blooming → Fully Grown
  - 다국어 시나리오
    - 언어 전환 (한국어 ↔ 영어)
    - 각 언어별 UI 텍스트 확인
    - 언어 설정 저장 확인

도구:
  - Playwright
```

---

## 🚨 알려진 이슈

### 현재 이슈
```yaml
기능:
  - Today 체크 시스템 미구현
  - 보상 지급 로직 미구현
  - streak 계산 미구현
  - 진화 시스템 미구현 (4단계 이미지는 준비됨)
  - 통계 산출 미구현

다국어:
  - i18n 설정 미구현
  - 언어 파일 (ko.json, en.json) 미작성
  - 언어 전환 UI 미구현
  - 모든 UI 텍스트 하드코딩 상태

데이터:
  - 로컬 스토리지만 지원
  - 멀티 디바이스 동기화 불가
  - 데이터 백업 기능 없음
  - User.language 필드 미구현

UI/UX:
  - Onboarding 화면 미구현
  - Goals/Projects 목록 미구현
  - 애니메이션 미구현 (진화)
  - 모바일 최적화 부족
  - 언어 전환 버튼/메뉴 미구현

에셋:
  ✅ 진화 단계 이미지 4종 준비 완료 (img/)
  - [ ] 에셋 최적화 필요 (WebP 변환)
  - [ ] 반응형 이미지 대응 필요
```

---

## 📝 CTO 합의 체크리스트

### 범위 확정
- [ ] MVP 범위 확정 (코칭/상점/배지 제외 동의)
- [ ] MVP+ 범위 확정
- [ ] V2 범위 확정

### 구현 방식 확정
- [ ] Today 체크 트랜잭션 (중복지급 방지) 방식 확정
- [ ] streak 계산 기준 확정 (로컬 날짜 기준)
- [ ] 통계 산정 방식 (일수 기준 vs 횟수 기준) 확정
- [ ] 레벨업 방식 (누적형 vs 차감형) 확정

### 화면 구성 확정
- [ ] 5개 화면 IA 확정
- [ ] 탭 네비게이션 구조 확정
- [ ] 우선순위 화면 확정

### 데이터 모델 확정
- [ ] 엔티티 구조 확정
- [ ] 관계 정의 확정
- [ ] 트랜잭션 원자성 보장 방식 확정

### 캐릭터 시스템 확정
- [x] 진화 단계 확정 (4단계: Egg → Sproutling → Blooming → Fully Grown)
- [x] 진화 레벨 조건 확정 (Lv 3, 8, 15)
- [x] 캐릭터 에셋 준비 완료 (img/ 폴더)

### 다국어 지원 확정
- [x] 지원 언어 확정 (한국어 기본, 영어 추가)
- [x] i18n 라이브러리 선정 (next-intl)
- [ ] 언어 파일 구조 확정
- [ ] 언어 전환 UX 확정
- [ ] 번역 워크플로우 확정

---

## 🔮 (보류) AI 코칭 스펙 메모 (V2)

> 이번 MVP에서는 제외. 추후 붙일 때의 가이드만 남김.

**입력**:
- 오늘 체크 요약
- 최근 7일 지표
- 선택 메모

**출력**:
- 칭찬 1문장
- 장애물 추정 1~2문장
- 내일 행동 1개

**저장**:
- 최근 30일만 (비용/프라이버시)

---

**Last Updated**: 2026-02-07
**Version**: 0.1.0 (MVP Planning)
